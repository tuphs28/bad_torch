To reproduce results:

1. Install BadTorch:

```bash
pip install -e .
```

2. Move in to the experiment directory, and run experiments.py

```bash
cd experiments
python3 experiments.py
```

3. To visualise the results, go to results_viz.ipynb